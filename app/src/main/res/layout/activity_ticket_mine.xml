<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="main.customizedBus.ticket.activity.TicketMineActivity">
    <main.utils.views.Header
        android:id="@+id/header"
        style="@style/Header"
        app:header_title="我的车票"></main.utils.views.Header>

    <RadioGroup
        android:id="@+id/id_radiogroup"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:orientation="horizontal">
        <RadioButton
            android:id="@+id/id_rbtall"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/white"
            android:text="全部车票"
            android:textColor="@color/button_tx_blue_gray_select"
            android:checked="true"
            android:textSize="18dip"
            android:button="@null"
            android:textAlignment="center"
            />
        <RadioButton
            android:id="@+id/id_rbtcomplete"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/white"
            android:text="已完成"
            android:button="@null"
            android:textColor="@color/button_tx_blue_gray_select"
            android:textSize="18dip"
            android:textAlignment="center"
            />
        <RadioButton
            android:id="@+id/id_rbtrefund"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/white"
            android:text="已退款"
            android:button="@null"
            android:textColor="@color/button_tx_blue_gray_select"
            android:textSize="18dip"
            android:textAlignment="center"
            />
    </RadioGroup>
    <FrameLayout
        android:id="@+id/id_framelayout"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"></FrameLayout>

</LinearLayout>